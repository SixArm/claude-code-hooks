# SessionEnd

This directory is for Claude Code `SessionEnd` hook examples.

This hook happens when an existing session is ending.

The `SessionEnd` event supports matchers on the reason the session ended.
